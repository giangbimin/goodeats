<%= image_tag food.image_url, alt: food.name.to_s , class: 'img-responsive' %>
  <h4><%= food.name %> <span><%= number_to_currency(food.price, precision: 0, separator: '.', delimiter: '.', unit: '') + ' VND' %></span></h4>
  <p><%= food.description %></p>
  <p><%= food.section %></p>
