<% if @order.persisted? %>
  location.href = '<%= done_order_show_path(id: @order.id) %>';
<% else %>
  $("#order-error").html("<%= escape_javascript(render 'shares/error', object: @order) %>");
<% end %> 