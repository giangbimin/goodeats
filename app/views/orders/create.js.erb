<% if @order.persisted? %>
  location.href = '<%= thank_you_show_path(id: @order.id) %>';
<% else %>
  $("#order-error").html("<%= escape_javascript(render 'shares/error', object: @order) %>");
<% end %> 